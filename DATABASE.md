# 数据库与评审呈现说明

## 当前状态

项目已经接入真实SQLite持久化，不再只依赖页面临时数据。本地完整服务首次启动时会依据 `database_schema.sql` 创建 `runtime/guiyang_elders.db`，并导入脱敏初始数据。机构端、老人端和监管端读取同一份业务状态。

## 数据架构

| 业务对象 | 数据表 | 主要用途 |
| --- | --- | --- |
| 接入机构 | `institutions` | 机构信息、质量分、预警与闭环指标 |
| 用户角色 | `users` | 测试账号和密码摘要 |
| 老人档案 | `residents` | OneID、来源地、房间、风险和快筛摘要 |
| 旅居关系 | `stays` | 老人与机构、城市、起止时间和在住状态 |
| 动向台账 | `movement_records` | 去向、返回计划、责任人、双重确认和关联事件 |
| 服务台账 | `service_records` | 服务类型、执行人员、时间和结果 |
| 风险事件 | `risk_events` | 风险等级、责任单位、时限和处置状态 |
| 处置过程 | `event_timeline` | 风险事件各时间节点及办理结果 |
| 数据授权 | `consents` | 老人对不同数据用途的授权状态 |
| 操作审计 | `audit_logs` | 操作者、角色、动作、对象编号和发生时间 |
| 兼容快照 | `app_snapshot` | 保证现有三端页面能够完整恢复业务状态 |

`app_snapshot`负责前端状态无损恢复，规范化表负责查询、监管统计、审计和后续系统对接。两者在同一事务中更新，写入失败时整体回滚。

## 评委现场怎么呈现

不建议制作数据库管理后台。数据库价值应通过一条可验证的业务闭环呈现：

1. 机构端新增外出报备，填写老人、目的地、返回时间、责任人和电话。
2. 监管端刷新“动向监管”，确认刚新增记录并留下核验备注。
3. 监管总览点击“操作审计”，展示两条记录对应同一动向编号。
4. 重启服务后再次查看该动向，证明数据真实持久化。
5. 说明超时未归可关联风险事件，返回核验可继续更新同一责任链。

这一过程同时证明数据写入、跨角色共享、状态流转、责任追溯和持久化，应用价值比展示表结构截图更强。

## 数据安全边界

- SQLite文件、WAL文件和DeepSeek密钥均被Git忽略，不会上传到GitHub Pages。
- 当前账号为公开脱敏测试账号，仅用于赛事评审；正式部署必须改为统一身份认证和会话令牌。
- 健康快筛为非诊断性风险提示，原始音视频默认不入库，人工确认结果才进入业务台账。
- 正式环境应增加最小权限、敏感字段加密、访问审计、备份恢复、日志留存期和数据删除机制。

## 正式部署路线

现场版本使用SQLite，优点是无需额外安装、断网可运行、数据库文件可直接备份。多机构正式试点建议迁移PostgreSQL，沿用现有实体关系，并将前端的整状态同步逐步替换为按业务对象增量写入接口。随后接入智桂通或主管部门统一身份认证、属地责任清单和应急联动渠道。
